<Page x:ConnectionId='1' x:Class="Template10.Samples.CortanaSample.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Behaviors="using:Template10.Behaviors"
      xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
      xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:controls="using:Template10.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:Template10.Samples.CortanaSample.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:Template10.Samples.CortanaSample.ViewModels"
      mc:Ignorable="d">

    <Page.DataContext>
        <vm:MainPageViewModel />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!--  header  -->
        <controls:PageHeader x:ConnectionId='2' BackButtonVisibility="Collapsed"
                             Content="Main Page"
                                                                
                             VisualStateNarrowMinWidth="-1">
            <AppBarButton x:ConnectionId='5'                                        
                          Icon="Setting"
                          Label="Settings" />
        </controls:PageHeader>

        <!--  #region content  -->

        <StackPanel Grid.Row="1" Padding="12,4,0,0">

            <TextBlock Style="{StaticResource HeaderTextBlockStyle}" Text="{Binding Value}" />

            <StackPanel Margin="0,12" Orientation="Horizontal">
                <Button x:ConnectionId='3'                                  Content="Speak" />
                <Button x:ConnectionId='4' Margin="12,0"
                                                                  
                        Content="Listen" />
            </StackPanel>

        </StackPanel>

        <!--  #endregion  -->

    </Grid>
</Page>

