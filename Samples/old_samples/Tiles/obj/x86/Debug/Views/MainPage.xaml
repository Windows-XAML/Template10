<Page x:ConnectionId='1' x:Class="Template10.Samples.TilesSample.Views.MainPage" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:Behaviors="using:Template10.Behaviors"
      xmlns:Core="using:Microsoft.Xaml.Interactions.Core" xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:controls="using:Template10.Controls" xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:Template10.Samples.TilesSample.Views" xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:Template10.Samples.TilesSample.ViewModels" mc:Ignorable="d">

    <Page.DataContext>
        <vm:MainPageViewModel />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <!--  adaptive states  -->

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:ConnectionId='2' x:Name="VisualStateGroup">
                <VisualState x:ConnectionId='3' x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger x:ConnectionId='6' x:Name="VisualStateNarrowTrigger" MinWindowWidth="0" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters />
                </VisualState>
                <VisualState x:ConnectionId='4' x:Name="VisualStateNormal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger x:ConnectionId='5' x:Name="VisualStateNormalTrigger" MinWindowWidth="520" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters />
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!--  header  -->
        <controls:PageHeader x:ConnectionId='7' BackButtonVisibility="Collapsed"                                     Content="Main Page">
            <AppBarButton Icon="Forward" Label="Forward">
                <Interactivity:Interaction.Behaviors>
                    <Behaviors:NavButtonBehavior x:ConnectionId='11' Direction="Forward"                                     />
                </Interactivity:Interaction.Behaviors>
            </AppBarButton>
        </controls:PageHeader>

        <!--  #region content  -->

        <RelativePanel Grid.Row="1" Padding="12,8,0,0">
            <TextBox x:ConnectionId='8' x:Name="ParameterTextBox" Width="200"
                     Margin="0" Header="Parameter to pass to this page"
                     Text="{Binding Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <Interactivity:Interaction.Behaviors>
                    <Behaviors:KeyBehavior Key="Enter">
                        <Core:InvokeCommandAction x:ConnectionId='10'                                                                   />
                    </Behaviors:KeyBehavior>
                </Interactivity:Interaction.Behaviors>
            </TextBox>
            <Button x:ConnectionId='9' Margin="4,0"                                                                  
                    Content="Submit" RelativePanel.AlignBottomWith="ParameterTextBox"
                    RelativePanel.RightOf="ParameterTextBox" />
        </RelativePanel>

        <!--  #endregion  -->

    </Grid>
</Page>

