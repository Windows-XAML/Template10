<Page x:Class="Sample.Views.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:Converters="using:Template10.Converters"
      xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
      xmlns:Interactivity="using:Microsoft.Xaml.Interactivity"
      xmlns:controls="using:Template10.Controls"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="using:Sample.Views"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:vm="using:Sample.ViewModels" RequestedTheme="Light"
      mc:Ignorable="d">

    <Page.Resources>
        <Converters:DateTimeFormatConverter x:Key="DateTimeFormatConverter" />
    </Page.Resources>

    <Page.DataContext>
        <vm:MainPageViewModel />
    </Page.DataContext>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">
                <VisualState x:Name="VisualStateNarrow">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NarrowMinWidth}" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="VisualStateNormal">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource NormalMinWidth}" />
                    </VisualState.StateTriggers>
                </VisualState>
                <VisualState x:Name="VisualStateWide">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="{StaticResource WideMinWidth}" />
                    </VisualState.StateTriggers>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="520" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--  header  -->
        <CommandBar Grid.Row="0" Grid.Column="1" Grid.ColumnSpan="2">
            <CommandBar.Content>
                <AppBarButton x:Name="BackButton" Icon="Back" Visibility="Collapsed" />
            </CommandBar.Content>
            <AppBarButton Command="{x:Bind ViewModel.DeleteCommand}" Icon="Delete" Label="Delete" />
        </CommandBar>

        <!--  #region content  -->

        <Grid Grid.Row="0" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="48" />
            </Grid.ColumnDefinitions>
            <TextBox Height="48" VerticalAlignment="Center"
                     BorderThickness="1" FontSize="26"
                     Text="{x:Bind ViewModel.SearchText, Mode=TwoWay}"
                     TextChanged="{x:Bind ViewModel.Search}" />
            <Button Grid.Column="1" Width="48" Height="48">
                <SymbolIcon Symbol="Find" />
            </Button>
        </Grid>

        <ListView Grid.Row="1" Margin="0"
                  Background="White"
                  ItemsSource="{x:Bind ViewModel.Messages, Mode=OneWay}"
                  Padding="0,4,0,0"
                  SelectedItem="{Binding ViewModel.Selected, Mode=TwoWay}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Margin" Value="0" />
                    <Setter Property="Padding" Value="0" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate xmlns:models="using:Sample.Models" x:DataType="models:Message">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="8" />
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Rectangle Fill="SteelBlue" />
                        <StackPanel Grid.Column="1">
                            <TextBlock x:Name="From" Margin="8,0,0,0"
                                       FontSize="24" FontWeight="Light"
                                       Foreground="Black" MaxLines="1"
                                       Text="{x:Bind From}" />
                            <TextBlock x:Name="Subject" Margin="8,0,0,0"
                                       FontSize="18" FontWeight="Normal"
                                       Foreground="{StaticResource CustomColorBrush}" MaxLines="1"
                                       Text="{x:Bind Subject}" />
                            <TextBlock x:Name="Body" Margin="8,0,0,8"
                                       FontSize="18" FontWeight="Normal"
                                       Foreground="DimGray" MaxLines="1"
                                       Style="{StaticResource TitleTextBlockStyle}" Text="{x:Bind Body}" />
                        </StackPanel>
                        <TextBlock x:Name="Date" Grid.Column="2"
                                   Margin="8,0,16,8" HorizontalAlignment="Right"
                                   VerticalAlignment="Bottom" FontSize="18"
                                   FontWeight="Light" Foreground="DimGray"
                                   Text="{x:Bind Date, ConverterParameter=hh:mm tt, Converter={StaticResource DateTimeFormatConverter}}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <x:String>Sample</x:String>
            <x:String>Sample</x:String>
            <x:String>Sample</x:String>
        </ListView>

        <!--  #endregion  -->

    </Grid>
</Page>
